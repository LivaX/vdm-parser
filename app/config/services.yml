# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
     vdm.manager.limit: 200

services:
    vdm.manager:
        class: AppBundle\Manager\VdmManager
        arguments: ["%vdm.manager.limit%"]
